# Forge PM Tools Service
# Self-hosted AI tool microservice — deployed separately on Vercel Hobby

# No external API keys needed — all tools are self-built using headless browsers
# and open-source parsers. Chromium binary is included via @sparticuz/chromium.

# Optional: set to production for optimized builds
NODE_ENV=development
